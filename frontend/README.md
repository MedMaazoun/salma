# ED Flow Intelligence — Frontend

Vite + React + TypeScript + Tailwind + Recharts + React Flow.

```
npm install
npm run dev
```

Dev server: http://localhost:5173. Requires the backend running on http://localhost:8000 (override with `VITE_API_URL`).
